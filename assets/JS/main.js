const timerEl = document.getElementById('timer')
const questionEl = document.getElementById('question')
const countdownEl = document.getElementById('timer')
const answersEl = document.getElementById('question-list')

function timer() {
    var timeLeft = 60
}